# A-Line Skirt — FC-100 #33

Dartless flare from the straight skirt block: the waist suppression is swung
into the curved side seam, which flares from the hip point to a hem `flare_mm`
wider than the hip half-width. Front on fold; back cut 2 with a CB zipper
notch. Parameter contract: `project.json` + the `main.py` docstring.
