# Crewneck Sweatshirt — FC-100 rank #6

Boxy felpa/fleece crewneck. Reuses rank #1's solved sleeve cap and extends
the derived-band rule to three ribs: neckband, cuffs (×2), hem band — each
length = measured opening × rib recovery ratio. Suggested fabric:
`materials/felpa-algodon` (body) + 1x1 rib for the bands.

```bash
python apps/api/services/engine/fc_runner.py projects/sweatshirt-crewneck/main.py sweat.svg '{}' svg
```
