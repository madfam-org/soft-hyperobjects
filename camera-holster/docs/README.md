# Camera Holster

The top-loading camera holster: a padded pouch deep enough for a body with a lens mounted, tapered at the base to the lens barrel so nothing rattles. A shaped lid closes over the mouth, and a swivelling snap hook anchors the holster to a belt loop or a pack's daisy chain so it moves between carries without unthreading anything. FC-300 (technical & outdoor).

Part of the **Fashion Cabinet Commons** (FC-300, technical & outdoor).
Official visualizer and configurator: [Fashion Cabinet](https://fashioncabi.net).

The swivelling snap hook is a **Yantra4D** hard good (`carabiner`, via `notion.hardware_ref`) — point hardware with no sewn flange, so it needs no edge coupling; `anchor_width` drives its inner width and the garment's anchor tab alike.

Built on the house boxed-wrap construction (`projects/dopp-kit`, `projects/belt-bag`): one panel is front + base + back folded at the base, with the corners boxed. The side gusset is **solved** so that `front_edge + base + back_edge` equals the body's side edge exactly, making the declared gusset seam a dimensional proof.

> A holster cut to the camera someone actually carries, with a printable snap hook, replaces the padded bag bought for a body sold three generations ago — and it can be re-cut around new gear instead of adding another case to the pile.

## Español
La funda de cámara de carga superior: una bolsa acolchada lo bastante profunda para un cuerpo con lente montado, afinada en la base al barril del objetivo para que nada suene.

> Una funda cortada a la cámara que alguien realmente carga, con un mosquetón imprimible, reemplaza la bolsa acolchada comprada para un cuerpo vendido hace tres generaciones.

## Français
L'étui photo à chargement par le haut : une poche rembourrée assez profonde pour un boîtier avec objectif monté, fuselée au fond au diamètre du fût pour que rien ne bouge.

> Un étui coupé au boîtier que l'on porte réellement, avec un mousqueton imprimable, remplace le sac rembourré acheté pour un appareil vendu il y a trois générations.

## Pieces
| Piece | Cut | Notes |
|---|---|---|
| `body` | 1 | Front + base + back folded at the base; boxed corners, pad zone |
| `side` | 2, mirrored | Side gusset solved to the body's side run; mouth wedge marked |
| `lid` | 1 | Shaped top lid with its fold line |
| `anchor` | 1 | Webbing tab carrying the swivelling snap hook |

## Provenance
Original draft for Fashion Cabinet. `CERN-OHL-W-2.0`.
